int a, b, c;
int[] f[12];

char a, b, c;
char[] f[12];

double a, b, c;
double[] f[12];

float a, b, c;
float[] f[12];

string a, b, c;
string[] f[12];

boolean a, b, c;
boolean[] f[12];

a = (a + b) * (1.5 + 10 * x);

if ( a + b ) {
    a = a + b;
}
else if (x)
{
   a = b;
}

switch (a) {
    case 5 :
		break;
    case 10 : 
		a = 10+10;
		a = 20 + 1;
		b = 1;
		break;
	default:
		break;
}

