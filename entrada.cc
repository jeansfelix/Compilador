void escreveLetra(int a, string c) 
{
    a = c;
}


int main(int argc, char argv)
{
    vetor[12] = a;

    a = a or b and c[a + b];
    
    a = 1 + 3;
        
    a = not c;
    
    b = v equal c;

    c = 5 % ++f * 3 + 2 ;
    
    if ( a + b ) {
        a = a + b;
    }
    else if (x)
    {
       a = b;
    }

    a = 10;

    for ( a=2 ; a < 3 ; a = a + 1) 
    {
        b = 1;
    }
    
    while (a == b) 
    {
        for ( a=2 ; a < 3 ; a = a + 1) 
        {
            b = 1;
        }
    }
    
    do {
        a = c + v;
    }while (a == b);

    switch (a) {
        case 5 :
		    break;
        case 10 : 
		    a = 10+10;
		    a = 20 + 1;
		    b = 1;
		    break;
	    default:
		    break;
    }
    
    return 0;
}

