a = (a + b) * (1.5 + 10 * x);

if ( a + b ) {
    a = a + b;
}
else if (x)
{
   a = b;
}

switch (a) {
    case 5 :
		break;
    case 10 : 
		a = 10+10;
		a = 20 + 1;
		b = 1;
		break;
	default:
		break;
}

